// Ported verbatim from the previous .prettierrc (JSON) — same style
// values, now in ESM flat-config form to match the rest of the toolchain.
export default {
    semi: true,
    trailingComma: 'none',
    singleQuote: true,
    printWidth: 100,
    tabWidth: 4,
    useTabs: false,
    arrowParens: 'always',
    endOfLine: 'lf',
};
